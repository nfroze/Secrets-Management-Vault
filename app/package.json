{
  "name": "secrets-vault-demo",
  "version": "1.0.0",
  "description": "Demo application consuming secrets exclusively through Vault-managed Kubernetes Secrets",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.12.0",
    "axios": "^1.6.7"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
